# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
node_modules/
**/node_modules/ # Crucial for ignoring node_modules in subdirectories (like your client/ and individual microservices)

# Build artifacts
dist/
**/dist/
build/
**/build/
.cache/
public/build/ # Specific for your client if it builds into public/build

# Environment variables
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor directories and files
.vscode/
!.vscode/extensions.json # Keep this if you want to share recommended VS Code extensions with others
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.bak
*.orig
*.swp
*~

# Operating System files
.directory
Thumbs.db

# IDE specific files
*.iml

# Frontend specific (React with Vite)
.vite/ # Vite's cache directory

# Compiled output for client (already covered by build/ and dist/ above, but good to be explicit if needed)
client/dist/

# MongoDB Compass related files (if any local configs are generated)
.mongodb/
